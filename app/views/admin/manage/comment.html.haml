%h1.text-center コメント一覧画面

%table.table
  %thead
  %tr
    %th{:scope => "col"} コメントid
    %th{:scope => "col"} コメント者
    %th{:scope => "col"} コメント先の物件
    %th{:scope => "col"} 内容
    %th{:scope => "col"} 日時
  %tbody
    - @comments.each do |com|
      %tr 
        %td= com.id
        %td= com.user.name
        %td= com.space.name
        %td= com.content
        %td= com.created_at
        
= paginate @comments
